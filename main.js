import { add, subtract } from './math-utils.js';

const sum = add(10, 5);
const difference = subtract(20, 7);

console.log(`The sum of 10 + 5 is: ${sum}`);
console.log(`The difference of 20 - 7 is: ${difference}`);
